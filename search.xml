<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>OO_Unit4 大结局</title>
      <link href="/2036/06/01/oo_U4/"/>
      <url>/2036/06/01/oo_U4/</url>
      
        <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><h2 id="Part-1">Part 1</h2><h3 id="两阶类图在正向建模过程中的作用">两阶类图在正向建模过程中的作用</h3><p>OO_pre 的惨痛教训告诉我在着手写代码之前理清楚架构是非常有必要的，但具体怎么理是个问题。我在前几个单元的训练中已经形成了一套自己的设计习惯，大致上是设计文档 <code>-&gt;</code> 手绘草图 <code>-&gt;</code> 代码骨架 的流程，所以刚开始接触 UML 这种很高级但很死板的模式就让我有点消化不良了。所以我依旧遵守了自己的设计习惯，把 UML 的绘制放到了最后的最后 —— 完全由 AI 完成。</p><h3 id="架构-对比分析最终的代码设计和UML模型设计之间的追踪关系">架构 &amp;&amp; 对比分析最终的代码设计和UML模型设计之间的追踪关系</h3><p>图书管理系统涉及到两方：人和图书馆，书（<code>BookCopy</code> 类）和预约（<code>Appointment</code> 类）被他们用来周旋，<code>Library</code> 类作为人和图书馆之间的沟通渠道，一方面管理图书馆麾下的 <code>BorrowReturnOffice</code>、<code>Bookshelf</code>、<code>AppointmentDesk</code> 和人员信息表 <code>UerTable</code>，一方面为人类读者提供各种办理业务的接口。</p><p>![](C:\Users\legion\Pictures\Screenshots\屏幕截图 2026-06-25 223619.png)</p><p>我的 UML 是跟着代码一起更新的，每个 HW 只在最后更新一次。</p><h3 id="大模型辅助正向建模">大模型辅助正向建模</h3><p>我很快意识到大模型不太会画 UML，而且鉴于我没有去学 UML 的规则，我也没法指导大模型学习。<br>把学长/学姐的 UML 源码和我写好的代码一起喂给 AI 是一个相当不错的选择，虽然很偷懒。</p><h2 id="Part-2">Part 2</h2><h3 id="架构设计思维的演进">架构设计思维的演进</h3><p>其实这个单元有点让我想起来了先导课的魔法大陆，都很重视代码的层次、功能约定、和接口。我记得当时代码设计的一团乱，什么方法、功能都乱糟糟的堆在一起，更别说层次化设计了，最后 IDEA 导出的类图让人看一眼就掉 san。值得庆幸的是，在经过一个学期的学习后，在递归下降和多线程的折磨后，区区一个功能式的系统已经不足以让我茫然无措了。</p><h3 id="测试思维的演进">测试思维的演进</h3><p>在这个学期开始之前，我对评测机的实践水平停留在计组的评测机，也就是少量的模块化指令和跳转指令生成 加 大量的无脑枚举，靠暴力生产所有指令组合。</p><p>表达式月的评测机（这是唯一一个非指令流式的），唯一一个纯手搓的评测机，我学会了分析各种错误点，主动设计引爆隐藏 bug 的表达式模式，撰写从弱测到强测的数据生成器，搭建结构完整（评测入口、测试模式选择、好看的 GUI）的评测机。</p><p>电梯月的评测机，这是一个多线程评测的评测机，同一个数据点会分别给每一份代码运行多次，允许线程间轮转，以模拟尽可能多的情况，应对错误难以复现的问题。问题是多线程导致不同数据点、不同 jar 包间存在互相影响（一个死锁导致其他测试也 TLE）的可能。</p><p>JML 月的评测机，这是一个针对 CTLE 的评测机，为了省事我甚至都没有赋予它正确性检测的功能。列出所有容易 CTLE 的指令，然后逐一设计诱导 CTLE 的场景，这又是一种与之前完全不同的模式。</p><p>UML 月的评测机，唯一一个纯 vibe 的评测机（甚至没有打开代码看过），感觉这是最简单的一个单元，也是最朴素的一个评测机。</p><p><img src="C:%5CUsers%5Clegion%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20260623211235878.png" alt="image-20260623211235878"></p><h3 id="综（含课程收获）">综（含课程收获）</h3><p>从表达式月兴致满满 + 精力旺盛、被程序架构可以达到的优美程度折服，到电梯月愉快地写了所有能想到或者看到的调度算法然后性能大比拼，到 JML 月接触了主题设计致力于给代码和 vscode 调色、学会撰写清晰（稍微有点长）的提示词而不是暴躁地跟 AI 吵架，到 UML 月力竭开始偷懒全职撰写提示词。贯穿了整个学期的面向对象为我带来极大的知识上和技术上的收获和精神上的愉悦。</p>]]></content>
      
      
      <categories>
          
          <category> cscore </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA_OO </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>OO_Unit3</title>
      <link href="/2036/06/01/oo_U3/"/>
      <url>/2036/06/01/oo_U3/</url>
      
        <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><h2 id="Part-1">Part 1</h2><h3 id="两阶类图在正向建模过程中的作用">两阶类图在正向建模过程中的作用</h3><p>OO_pre 的惨痛教训告诉我在着手写代码之前理清楚架构是非常有必要的，但具体怎么理是个问题。我在前几个单元的训练中已经形成了一套自己的设计习惯，大致上是设计文档 <code>-&gt;</code> 手绘草图 <code>-&gt;</code> 代码骨架 的流程，所以刚开始接触 UML 这种很高级但很死板的模式就让我有点消化不良了。所以我依旧遵守了自己的设计习惯，把 UML 的绘制放到了最后的最后 —— 完全由 AI 完成。</p><h3 id="架构-对比分析最终的代码设计和UML模型设计之间的追踪关系">架构 &amp;&amp; 对比分析最终的代码设计和UML模型设计之间的追踪关系</h3><p>我的 UML 是跟着代码一起更新的，每个 HW 只在最后更新一次。</p><h3 id="大模型辅助正向建模">大模型辅助正向建模</h3><p>我很快意识到大模型不太会画 UML，而且鉴于我没有去学 UML 的规则，我也没法指导大模型学习。<br>把学长/学姐的 UML 源码和我写好的代码一起喂给 AI 是一个相当不错的选择，不过很偷懒。<br>我在这里第一次接触了 skill 撰写，整合了 UML 绘制的基础要求与 OO 作业的特殊要求（关键词、MainClass）。</p><h2 id="Part-2">Part 2</h2><h3 id="架构设计思维的演进">架构设计思维的演进</h3><h3 id="测试思维的演进">测试思维的演进</h3><p>在这个学期开始之前，我对评测机的实践水平停留在计组的评测机，也就是少量的模块化指令和跳转指令生成 加 大量的无脑枚举，靠暴力生产所有指令组合。</p><p>表达式月的评测机（这是唯一一个非指令流式的），唯一一个纯手搓的评测机，我学会了分析各种错误点，主动设计引爆隐藏 bug 的表达式模式，撰写从弱测到强测的数据生成器，搭建结构完整（评测入口、测试模式选择、好看的 GUI）的评测机。</p><p>电梯月的评测机，</p><p>JML 月的评测机，这是一个针对 CTLE 的评测机，为了省事我甚至都没有赋予它正确性检测的功能。</p><p>UML 月的评测机，唯一一个纯 vibe 的评测机，</p><h3 id="综（含课程收获）">综（含课程收获）</h3><p>从表达式月兴致满满 + 精力旺盛、被程序架构可以达到的优美程度折服，到电梯月愉快地写了所有能想到或者看到的调度算法然后性能大比拼，到 JML 月接触了主题设计致力于给代码和 vscode 调色、学会撰写清晰（稍微有点长）的提示词而不是暴躁地跟 AI 吵架，到 UML 月力竭开始偷懒全职撰写提示词。</p>]]></content>
      
      
      <categories>
          
          <category> cscore </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA_OO </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>OO_Unit2</title>
      <link href="/2036/06/01/oo_U2/"/>
      <url>/2036/06/01/oo_U2/</url>
      
        <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><h1>OO_Unit2</h1><p>[TOC]</p><h3 id="架构-迭代">架构 &amp;&amp; 迭代</h3><hr><img src="D:\buaa_study\second_year_2\OO\HW8\电梯架构图.png" style="zoom:150%;" /><p>在 <strong>如何让系统结束</strong> 上，我起初采用了递进式 kill：<code>MainCtrl</code> 判定总队列空且已经输入了 NULL，就 kill 自己并把信号传递给所有电梯，电梯接收到 kill 信号后根据自身进度决定什么时候结束。这个方法很不靠谱，导致我互测被刀了。我后来根据水群里大佬们的分享改成了计数器：维护一个 <code>cntRest</code> 之类的东西，+1，-1，当 <code>cntRest == 0</code> 并且已经输入了 NULL 就全终止，优雅又省心。</p><h3 id="锁-同步块">锁 &amp;&amp; 同步块</h3><hr><blockquote><p>总结分析三次作业中同步块的设置和锁的选择，并分析锁与同步块中处理语句之间的关系</p></blockquote><h3 id="调度器设计">调度器设计</h3><hr><p>我在 HW6 一共写了 4 种调度器算法，在 HW6 跑了 2000 轮数据排名：影子电梯 &gt; (自由竞争，随机) &gt; 均匀。所以我在 HW7 只迭代了影子电梯。</p><p>我没有尝试纯启发式，但互测的时候看到我们房多数人用了纯启发，不过没有发现性能很好的。不知道有没有人纯启发很成功的。</p><h4 id="均匀分配">均匀分配</h4><p>均匀分配全方位倒第一，所以很快就被淘汰了。</p><h4 id="Random">Random</h4><p>随机性能挺好的（但远远不如影子和自由竞争），而且（&gt;50组数据时）很稳定，但它有一个让人很难受的问题：它没办法设计预定队列，所以想避开一次性维修 5 台电梯压力最后一台电梯只能靠接收上限，但这样写不好容易搞出来轮询。</p><h4 id="自由竞争">自由竞争</h4><p>这是一种下限很低，但上限很高的算法，如果实现的好的话可能仅次于完善的影子电梯。但不知道为什么我写出来的自由竞争显著倾向于节省电量（比影子电梯省 1/3），时间性能不好（比影子电梯多 1/4），后来渐渐放弃了。</p><p>核心思路是计算所有电梯接到这个乘客的时间加以比较。这个方案是如何处理时长与电量之间的关系的？我感到很迷惑。我只是机械的操作了 “计算接到乘客的时长”，我没有想为什么最快接到人的就是最好的。如果说是从 “最快接到” 的角度让 “平均时长” 尽可能的短……可我写出来效果不是这样的。</p><p>自由竞争里也有小模拟，和影子电梯有一点点像，只是轻量很多，也不用起线程。另外这好像是唯一一个能直接反映 “平均时长” 的算法，影子电梯完全没法管 “平均时长“ 的。</p><h4 id="影子电梯">影子电梯</h4><p>这个上限更高，也更让人越优化越绝望。影子电梯理论上可以获取最佳的运行方案，但考虑到不断有乘客到来，影子电梯不可能做到真正的一比一复刻，即使想做到高度复刻也是很困难的。还有 “平均完成时间” 的概念完全无法计算，只能考虑总时长和电量然后研究怎么加权。</p><p>我的影子电梯的核心思路是假设当前乘客是最后一名乘客，计算选中的电梯送完所有人的时间 cost 和耗电量 power。有关 cost 和 power 的权重我想了很久，最终得出的结论是我不应该给 power 任何权重，因为这个 power 跟新加的人相关性实在太小了。（我尝试分别赋 10:0、9:1、8:2、……、1:10 的权重跑了几个 100 轮，跑出来的名次不稳定，但总的来说差距比较小，而且当时我的代码还有 2/1000 左右的 bug，就不了了之了）。</p><p>在 HW6 我的影子电梯运行策略与原电梯完全一致，对于正在维修的电梯，我设置了一个 <code>preQueue</code> 用来存要分配给他但不能输出 RECEIVE 的乘客，这样一来就不存在不能接人的电梯，所以影子电梯的维修时行为也和原电梯完全一致。</p><p>在 HW7，我总共设计了 4 种模拟双轿厢的影子：</p><ul><li>给广义电梯类（或者说电梯井类）也写影子，然后全模拟</li><li>限制电梯的运行范围，然后统计所有没到达目的地的乘客距离目的地的距离，除以 3 （核载人数-1）然后 × 一个数字</li><li>统计 time/perFloor,perPerson</li><li>假设电梯可以运行全程，然后罚时（×权重）</li></ul><p>理论来讲最好的应该是第一种，但我肯定是哪儿写错了，效果不如最后一种。所以说我写了一大圈，最后交的是只需要加半行的策略……所以说影子电梯它很让人绝望。</p><p>不过总的来说性能挺好的。</p><h3 id="手动构造数据策略">手动构造数据策略</h3><hr><p>吸取上一个月的教训，不能完全依赖数据生成器。我采用以下几点策略尽可能地强化数据点。</p><ul><li>压力某一台电梯：通过维修、设计距离之类的方法试着把人逼上同一台电梯，然后就离 RTLE 不远了。</li></ul><h3 id="bugs">bugs</h3><hr><p>在 HW5 强测出现一个 bug，互测也是同一个 bug；HW6 互测出现一个 bug；HW7 没有 bug。</p><h4 id="HW5-强测-互测">HW5 强测 &amp;&amp; 互测</h4><p>强测 WA 了一个点。很奇怪。在电梯月 WA 了。</p><p>问题出在：<strong>必须先 RECEIVE 后加入队列！！！</strong></p><p>我猜测这个限制或许是为了 ban 掉量子电梯抢跑，因为如果不强制要求先 RECEIVE 后采取任何行动的话，评测机无法得知在 RECEIVE 乘客前的那 0.4s 电梯在做什么，也就是说乘客可以同时 向上/不动/向下（薛定谔的量子电梯），而且这似乎是唯一一种确定可以提升效率的量子电梯。但它也同时 ban 掉了先加入队列后输出 RECEIVE 的人，因为加入队列后电梯就可以行动了，但这时可能还没来得及输出 RECEIVE。于是我这个二百五就这样被打了。</p><p>当然虽然这个点真的很蠢，但我会 WA 根本上还是因为不习惯从多线程的角度思考问题，对并行中可能出现的问题不够敏感（是完全没有知觉，我天真的以为搞定同步互斥问题就 ok 了），不然也不会在题目明确要求的情况下仍然随随便便写这两行代码。</p><p>不难发现，当时我的评测机也并没有针对 RECEIVE 顺序的测试。</p><h4 id="课下-bug-0">课下 bug 0</h4><p>我在 HW6 及以前没有给电梯设任何楼层范围的限制（按理说用不着），但测试 HW6 的时候莫名卡死。经检查发现竟然是我的影子电梯飞天遁地了！因为电梯快照照的不同步导致出现了 <code>weight != 0</code> 但 <code>destMap</code> 空着的尴尬情况，我的 LOOK 又是很纯粹的接不到反向乘客、<code>weight</code> 不为 0 就一直走的 LOOK，根本停不下来……</p><p>解决方案：我把电梯快照改成了原子操作，这样虽然保证不了分配策略拿到最新数据，但起码能正常运行结束了，顶多就是数据不够新导致策略不太好。</p><h4 id="课下-bug-1">课下 bug 1</h4><p>这个 bug 没有以任何形式被测出来，是我静态臆想出来的，所以我不确定它是否真的有问题或者有可能被打到。</p><p>我继承自 HW5 的代码中，系统结束过程是递进式的：<code>MainCtrl</code> 判定总队列空且已经输入了 NULL，就 kill 自己把信号传递给所有电梯，电梯接收到 kill 信号后根据自身进度决定什么时候结束。这样在 HW5 很好，但在 HW6 不行，因为我处理维修电梯中乘客的方法是把它们丢回总队列，设想：</p><p>输入的最后一句是 MAINT 6 号电梯，<code>MainCtrl</code> 将该信号传递给 6 号电梯后自杀，6 号电梯歇了一会儿后开门把人都撵回主队列，结果发现主队列已经不复存在了，可怜的乘客成了孤魂野鬼。</p><p>解决方案是在 <code>MainCtrl</code> 的判定条件中加入当前是否有电梯在维修。不幸的是，这个解决方案引入了新的 bug：</p><h4 id="HW6-强测-互测">HW6 强测 &amp;&amp; 互测</h4><p>强测侥幸过了，互测被刀了 14 刀，玉衡星来给大家发福利啦 😉。</p><p>被刀中的原因是死锁。这个死锁来的猝不及防，因为我其实是地毯式检查过死锁的，但不幸的是这个死锁是在检查之后被引入的……</p><p>我的 <code>MainCtrl</code> 里有这样一段代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">Person</span> <span class="variable">person</span> <span class="operator">=</span> mainWaitQueue.getaRequest(); <span class="comment">// 从总队列里取一个人</span></span><br><span class="line"><span class="keyword">if</span> (person == <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">synchronized</span> (validElevatorLock) &#123; <span class="comment">// 检查一下是不是在检修，否则可能最后一个人刚下电梯，MainCtrl 就下班了</span></span><br><span class="line">        <span class="keyword">while</span> (hasMaintainingElevator()) &#123;</span><br><span class="line">            ......</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个 <code>hasMaintainingElevator</code> 调用了 <code>synchronized boolean isMaintaining()</code>，而 <code>validElevatorLock</code> 是个全局变量锁，这样就诞生了一个教科书式的所顺序反转。而这个 <code>validElevatorLock</code> 是我在 debug 过程中加入的，目的是维护 “整个系统中是否有电梯正在维修” 状态的一致性，设置这个状态的目的是为了解决 bug1。</p><p>或许可以靠顺序修改解决这个问题，但我在修改的时候看到群里有人说采用计数器的方法来停掉 MainCtrl，也就是从根本上改掉我那个不靠谱的递进式 kill，这个方法明显比我那个优雅的多，所以我改成了计数器，这个修改让我得以顺利度过 HW7。</p><h4 id="HW7-强测-互测">HW7 强测 &amp;&amp; 互测</h4><p>没有刀，但其实有一处很危险。</p><p>因为我的影子电梯是可以预定的，在维修状态的电梯可以把人收进预队列里，所以我就没写一个电梯接受人数上限的设置。但我的双轿厢罚时设计的不好，所以 5 台电梯升级加同时爆发大量请求可以把人都逼到同一台电梯里，最长可达 180.059 s 左右。不难想象这个点有多难打，不知道是不是有人发现了但刀不中。</p><p>HW7 房间有个人大约有 15% 的数据点维修超时或者升级超时，但我始终没能刀中。</p><h3 id="线程安全-层次化设计">线程安全 &amp;&amp; 层次化设计</h3><hr><ul><li>没有绝对安全的线程……当然是假的，但我 de 同步问题 de 的很痛苦。</li><li>或许可以给电梯设计一个基类，然后让 1~6 电梯和影子电梯都继承它，这样就不要每次都费劲让它们保持一致了。</li></ul><h3 id="大模型">大模型</h3><hr><ul><li>GPT 从 HW5 开始写的评测机和数据生成器，一直写到 HW7。每次把迭代内容输入/输出、数据强化思路总结给它。好处是写的快、省事儿而且足够强，坏处是从 HW6 开始我就看不懂也懒得看了。这个评测机测出了我绝大多数的 bug，从 5/100 RTLE 到 10000/10000 AC。</li><li>我让 GPT 写了一个根据输入/错误的输出自动生成格式化报告的程序，总结出程序结束（其实是卡死）时 <code>MainQueue</code> 中的人和所有电梯没送完的人，以便于检查死锁。</li><li>代码是古法的。</li></ul><h3 id="心得体会">心得体会</h3><hr><h3 id="可能的建议">可能的建议</h3><hr><ul><li>诚挚地建议提高电梯月互测中的代码运行次数，哪怕 2 次也可以……刀不到的 bug 太难受了……这样也可以变相减少总的提交次数</li><li>希望有关 “恶意 hack 同质 bug” 可以有 <strong>更明确的说明</strong>，比如 <strong>保证在被判定为恶意 hack 之前会有助教联系确认情况</strong> 之类的……因为很多时候都存在：“A 和 B 有相同的问题，但 A 的问题更严重，我可能永远刀不到 B，但我会稳定地误伤 A” 的问题。</li><li>助教哥哥姐姐们辛苦了。</li></ul>]]></content>
      
      
      <categories>
          
          <category> cscore </category>
          
      </categories>
      
      
        <tags>
            
            <tag> OO </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>OO_Unit1</title>
      <link href="/2036/06/01/oo_U1/"/>
      <url>/2036/06/01/oo_U1/</url>
      
        <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><h2 id="架构-迭代">架构 &amp;&amp; 迭代</h2><img src="/img/OO/U1/class.png" width="700"><p>应用最显著的是递归下降的设计思路。</p><p>我的代码分为三块儿大陆：<code>Parser</code> 负责解析，<code>ast</code> 负责整理，<code>algebra</code> 负责输出。三块儿大陆各司其职，立志于不做重复的工作。<code>Parser</code> 里的 <code>Expr</code> 类是它们的交通枢纽。</p><p>定义两个数据结构：<code>ast</code> 语法树（以递归下降的形式存储表达式），和 <code>algebra</code> 多项式（以标准输出结构的形式存储表达式）。</p><p>如图：尾缀带 <code>Factor</code> 都是继承 <code>Factor</code> 类的（这里无疑用接口更好，但问题不大就懒得改），其中 <code>NumFactor</code> 和 <code>VarFactor</code> 是叶子<code>Factor</code>，别的都是非叶 <code>Factor</code>。<code>Expr</code> 很特殊，它既包含非叶 <code>Factor</code> 的方法，又包含根节点的方法。</p><p><code>Factor</code> 需要实现的方法，最初只有一个：<code>toPoly</code>。</p><img src="/img/OO/U1/ast.png" width="400"><p><code>algebra</code> 的标准输出形式最初是 <code>num*x^exponent</code>。</p><p>对于输入的表达式字符串，我首先通过 <code>Parser</code> 将它转化成 <code>ast</code> 的形式来存储。<code>ast</code> 是一棵树，它会自动把表达式整理成递归下降的结构，可以通过根节点 <code>Expr</code> 访问。在 <code>Expr</code> 实现 <code>toPoly</code> 方法，把整理好的语法树导出成多项式的形式，此时已经到达了 <code>algebra</code> 大陆的 <code>Poly</code> 类。在 <code>Poly</code> 实现合并同类项方法和 <code>toString</code> 方法，将结果导出成字符串并输出。</p><p>以上是我的基本思路，具体实现在三次迭代中逐渐填充。</p><p>在迭代中要求实现的功能可以 <strong>按实现路径</strong> 分为 2 类，<strong>函数定义</strong> 和 <strong>函数调用</strong>。前者包括 HW2 中自定义函数的定义和 HW3 中递归函数的定义，实现方式均为通过与之前无异的方法存储一棵语法树作为模板。后者包括 HW2 中 自定义函数、指数函数、选择表达式（函数）的调用，和 HW3 中递归函数，求导（函数）的调用，实现路径为 <code>识别输入字符串中的特定字符 -&gt; 用解析出的实参（因子或者表达式）替换模板中的自变量 -&gt; 把生成的子树挂在目标表达式树中的对应位置</code>，从这个角度想，那个复杂的自定义函数语法树，与 <code>ExpFactor</code> 类没有任何实质上的区别，只是一棵大树一棵小树罢了。</p><h3 id="第一次迭代">第一次迭代</h3><p>就是那个骨架。</p><h3 id="第二次迭代">第二次迭代</h3><div class="note default flat"><p>功能一：自定义函数的 <strong>定义</strong> 与 <strong>调用</strong>。<br>功能二：指数函数的 <strong>调用</strong>。<br>功能三：选择表达式的 <strong>调用</strong>。</p></div><p>需要注意深拷贝的问题，因为函数可以多次被调用，使用浅拷贝会得到几棵纠缠在一起的树。</p><h3 id="第三次迭代">第三次迭代</h3><div class="note default flat"><p>功能一：递归函数的 <strong>定义</strong>。<br>功能二：递归函数地 <strong>调用</strong>。<br>功能三：添加 <code>y</code><br>功能四：求导函数的 <strong>调用</strong></p></div><p>有关用来求导的数据结构：<br>我的第一反应就是在 <code>ast</code> 里递归下降求导，研讨课时发现多数人采用的在 <code>Poly</code> 里求导，这样对于 <code>dx(x-x)</code> 等情况会更快。</p><p>其实我有一个异想天开的想法，既然给这个项目加功能的路径就那么两条，那能不能实现一个 “<em>加功能</em> 功能” 呢？统一功能接口，然后在加功能的时候写回调函数。当然，这个纯属异想天开。</p><hr><table><thead><tr><th>Method</th><th>CogC</th><th>ev(G)</th><th>iv(G)</th><th>v(G)</th></tr></thead><tbody><tr><td>Main.main(String[])</td><td>2</td><td>1</td><td>3</td><td>3</td></tr><tr><td>algebra.MonoKey.MonoKey(BigInteger, BigInteger, Poly)</td><td>0</td><td>1</td><td>1</td><td>1</td></tr><tr><td>algebra.MonoKey.equals(Object)</td><td>3</td><td>3</td><td>3</td><td>5</td></tr><tr><td>algebra.MonoKey.getExpArg()</td><td>0</td><td>1</td><td>1</td><td>1</td></tr><tr><td>algebra.MonoKey.getXexponent()</td><td>0</td><td>1</td><td>1</td><td>1</td></tr><tr><td>algebra.MonoKey.getYexponent()</td><td>0</td><td>1</td><td>1</td><td>1</td></tr><tr><td>algebra.MonoKey.hashCode()</td><td>0</td><td>1</td><td>1</td><td>1</td></tr><tr><td>algebra.MonoKey.mulMonoKey(MonoKey)</td><td>0</td><td>1</td><td>1</td><td>1</td></tr><tr><td>algebra.Poly.Poly()</td><td>0</td><td>1</td><td>1</td><td>1</td></tr><tr><td>algebra.Poly.add(Poly)</td><td>4</td><td>1</td><td>3</td><td>3</td></tr><tr><td>algebra.Poly.addToSelf(MonoKey, BigInteger)</td><td>3</td><td>2</td><td>2</td><td>3</td></tr><tr><td>algebra.Poly.appendMonoBody(StringBuilder, MonoKey, BigInteger)</td><td><font color  ="red">17</font></td><td>2</td><td><font color  ="red">12</font></td><td><font color  ="red">14</font></td></tr><tr><td>algebra.Poly.appendSign(StringBuilder, BigInteger, boolean)</td><td>6</td><td>1</td><td>3</td><td>4</td></tr><tr><td>algebra.Poly.buildExpText(Poly)</td><td>2</td><td>2</td><td>1</td><td>2</td></tr><tr><td>algebra.Poly.constant(BigInteger)</td><td>1</td><td>1</td><td>2</td><td>2</td></tr><tr><td>algebra.Poly.equals(Object)</td><td>3</td><td>3</td><td>3</td><td>4</td></tr><tr><td>algebra.Poly.hashCode()</td><td>0</td><td>1</td><td>1</td><td>1</td></tr><tr><td>algebra.Poly.isFollower(BigInteger, boolean, boolean)</td><td>1</td><td>1</td><td>3</td><td>3</td></tr><tr><td>algebra.Poly.isPureExpFactorLike()</td><td>6</td><td>2</td><td>2</td><td>7</td></tr><tr><td>algebra.Poly.isZero()</td><td>0</td><td>1</td><td>1</td><td>1</td></tr><tr><td>algebra.Poly.liftMono(Unit)</td><td>1</td><td>1</td><td>2</td><td>2</td></tr><tr><td>algebra.Poly.multiply(Poly)</td><td>3</td><td>1</td><td>3</td><td>3</td></tr><tr><td>algebra.Poly.negate()</td><td>1</td><td>1</td><td>2</td><td>2</td></tr><tr><td>algebra.Poly.packExp(StringBuilder, Poly)</td><td>0</td><td>1</td><td>1</td><td>1</td></tr><tr><td>algebra.Poly.positiveToFront(ArrayList<MonoKey>)</td><td>3</td><td>3</td><td>3</td><td>3</td></tr><tr><td>algebra.Poly.pow(BigInteger)</td><td>5</td><td>1</td><td>4</td><td>4</td></tr><tr><td>algebra.Poly.toString()</td><td>4</td><td><font color  ="red">4</font></td><td>2</td><td>4</td></tr><tr><td>algebra.Poly.zero()</td><td>0</td><td>1</td><td>1</td><td>1</td></tr><tr><td>algebra.Unit.Unit(MonoKey, BigInteger)</td><td>0</td><td>1</td><td>1</td><td>1</td></tr><tr><td>parser.Lexer.getNumber()</td><td>2</td><td>1</td><td>3</td><td>3</td></tr><tr><td>parser.Lexer.getTokenType(char)</td><td>1</td><td><font color  ="red">18</font></td><td>1</td><td><font color  ="red">20</font></td></tr><tr><td>parser.Lexer.next()</td><td>6</td><td>2</td><td>4</td><td>6</td></tr><tr><td>parser.Lexer.peek()</td><td>0</td><td>1</td><td>1</td><td>1</td></tr><tr><td>parser.Parser.Parser(Lexer, Definer, DeepDefiner)</td><td>0</td><td>1</td><td>1</td><td>1</td></tr><tr><td>parser.Parser.parseExpr()</td><td><font color  ="red">17</font></td><td>1</td><td>8</td><td><font color  ="red">11</font></td></tr><tr><td>parser.Parser.parseFactor()</td><td><font color  ="red">19</font></td><td><font color  ="red">11</font></td><td><font color  ="red">13</font></td><td><font color  ="red">15</font></td></tr><tr><td>parser.Parser.parseTerm(int)</td><td>12</td><td>1</td><td>7</td><td>9</td></tr></tbody></table><p>我的 <code>ast</code> 类都写得很干净，这里就不贴出来了，主要是看 <code>Poly</code> 和 <code>Parser</code>。方法圈复杂度还勉强能看，我把 <code>toString</code> 拆分成几个功能模块之后就没有太恶心的东西了，但 <code>Parser</code> 还是让我很难受，这当然可以通过添加预处理类、降低 <code>ParseFactor</code> 内部的耦合度来解决，但我一直懒于修改。</p><p>不过好在这几个复杂度惊人的方法没给我招来 bug。</p><hr><h2 id="优化">优化</h2><ul><li>这优化可以拿去出国赛了。</li><li>HW1 只有一个正号提前……吧。</li><li>HW2 和 HW3 的优化点相同，都是 <code>exp</code> 内的 gcd 提取和 <code>exp</code> 拆分。<ul><li>我在 HW2 引入了 gcd 提取，提取思路是从 gcd 向下枚举因子，记录每个位数下的最大因子，得到一个不超过 10 个元素的候选集，然后暴力。这个方法有点笨，效果也很一般，但也是我想了挺久才想出来的，我最终找到一个性能最好而且在互测中不可能被 hack 的临界点，超过这个临界点就不记录、仅尝试 <code>gcd</code>、<code>gcd/2</code>……<code>gcd/100</code> 。我后来知道其实最优解一定出在 <code>gcd</code> ~ <code>gcd/0</code> 之间，可惜数学能力不够，完全没往这个方向想。</li><li>我在 HW3 引入了 <code>exp</code> 拆分，这个拆东墙南墙补西墙北墙的最优化问题，我想到的唯一一个解法是动态规划，实现的……还算可以，能看到效果。但是由于处理不完整造成了 bug，这个后面再说。</li></ul></li></ul><h2 id="几个显著的问题">几个显著的问题</h2><ul><li>组合优于继承，但我一开始就用了抽象类而非接口，后来懒得改。</li><li>没有预处理方法，事实证明 <code>PreProcesser</code> 的存在可以省下很大功夫，前两次还看不太出来，第三次就很闹心了。直接导致 <code>Lexer</code> 爆复杂度和 <code>DeepDefiner</code> 又丑又长。如果再有类似的东西我一定要写预处理。</li><li><code>exp</code> 类没有指数字段，也没有什么灵活的修改空间，间接导致我 HW3 优化出锅（因为优化完的东西没法存）。</li></ul><h2 id="自动化评测方法">自动化评测方法</h2><p>我纯古法的数据生成器。采用递归下降+模块式生成的方法。写生成器的过程中遇到了很多问题：递归以及递归下降层数限制、<code>Expr</code> 一类两用、是否允许 <code>y</code>、是否允许选择式……我最终给模块式生成整理出一套参数：<code>(depth, isFactor, allowFunc=True, allowChoose=True, allowDeri=True, allowRec=True, allowY=True)</code>，以更好地复用函数。</p><p>这个评测机在 HW1 中找出了自己的一个 bug，在 HW2 中找出了两位同学的 bug。但它有诸多不足，他没能测出 HW2 强测我的 bug，也没能测出 HW2 中我室友的 bug，前者是由于那个 bug 就是不好用自动化方法测出来，后者是因为我的数据生成器生成的选择表达式 <code>==</code> 两边只有同类型数据。</p><h2 id="bugs-互测">bugs &amp;&amp; 互测</h2><p>在 HW2 强测中出现 1 个 bug。在 HW3 互测中优化出 2 个 bug 被刀了 4 次。HW2 有效 hack 9 次，HW3 有效 hack 3 次。</p><h3 id="HW2">HW2</h3><p><strong>强测</strong>  WA 了一个点，WA 进 b 房了😿：实在有点愚蠢，我把 <code>PowFactor</code> 里的指数改成了 <code>BigInteger</code>，但 <code>Poly</code> 里还是 <code>int</code>，面对 <code>(((((((((x^8)^8)^8)^8)^8)^8)^8)^8)^8)</code> 理所当然的溢出了。</p><p>我需要反思一下，为什么我的评测机没有查到这个点呢？其实是我的数据生成器太随机了，我只按照题目的要求写了格式规范，至于它具体生成什么全权交给 <code>random</code> 决定了。当然，我也有通过改变权重、改变常数范围的方式加强我的评测机，但幂次足够高的数据往往伴随着爆代价导致的 TLE，因此我 <code>Poly</code> 里使用 <code>int</code> 的 bug 完全被 TLE 掩盖了。</p><p>其实也怪我懒于手动构造数据，但我清晰地记得我刚进 HW2 就把 <code>PowFactor</code> 里的数据类型给改了，所以根本没想过自己会在这里出问题。还有，这种 “不对称” 问题 AI 应该是很好查的，我下次要交之前让 AI 给我盯一盯。</p><p><strong>互测</strong>：像 <code>(((((((((x^8)^8)^8)^8)^8)^8)^8)^8)^8)</code> 这种点在互测是非法的，所以我没有在互测期间发现自己的 bug，也没被刀。</p><p>有个一穿 3 的点是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">1</span><br><span class="line">f(x)=(x+1)^8</span><br><span class="line">[(1==1)?1:f(f(f(f(f(f(f(f(f(f(f(f(f(f(f(f(f(f(f(f(f(f(f(f(f(f(f(f((x+1)))))))))))))))))))))))))))))]</span><br></pre></td></tr></table></figure><p>简单粗暴有效。制裁使用字符串替换（除了能写明白这个的大佬之外）/随意实例化的人。</p><p>另有一个一穿 4 的点是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">0</span><br><span class="line">[(0==0)?1:((((((x+1)^8)^8)^8)^8)^8)^8]</span><br></pre></td></tr></table></figure><p>这个 cost 计算很阴啊，我自己能过是幸运的，因为我到互测才知道这个问题。</p><p>还有一位代码处理不了连续符号的同学，他无法通过我的评测机的任何一个数据点，结果误伤了他好几次，私密马赛。</p><p>总之这个房间很欢乐。</p><h3 id="HW3">HW3</h3><p>强测没事儿。互测被 hack 惨了。</p><p>一个 bug 是很多人都中招了的，乘开优化少括号的问题。在 HW2 中，为了判断 <code>exp</code> 需不需要套一层括号，我写了一个 <code>isPureFactor</code> 方法，判断一个 <code>Term</code> 是否只是 <code>Factor</code>，判断方法为 <code>constExit + xExit + yExit + ExpExit &lt;= 1</code>。显然，对于 <code>exp -&gt; exp*exp</code> 的情况，这里会少一个括号。</p><p>另一个 bug 是类似 <code>exp((2*exp((2*exp((2*exp(x)))))))</code> 加函数嵌套。我在做优化的时候有考虑 TLE 的问题，所以我加了限制：项数增加到一定程度就不做优化了，可我没有考虑多层嵌套超时的问题。</p><p>不过没关系，优化开关一关，都是同质 bug😉。</p><h2 id="心得体会">心得体会</h2><p>我习惯于先设计架构，再把所有的类和空方法都建出来，最后再填充具体实现，这样看着自己设计的骨架一点点长出血肉，当然每次都会经历断骨再生就是了。虽然后续的迭代过程很小心，但还是眼睁睁看着我的代码变得奇形怪状。</p><p>我认为本次设计最大的优点是功能清晰，没有出现 <code>oopre</code> 中出现的 “不知道有什么用的类”、“走错类的方法” 和 “互抢生意的兄弟类（甚至父子类）”。最大的缺点在于方法内部存在大量冗余逻辑，导致一些方法（特指 <code>parseFactor</code> 和 <code>toString</code>）又丑又长，甚至 checkstyle 不过、被迫拆分。</p><p>这个月的 oo 命途多舛，听说下个月的电梯更阴间，还是在清明假期……要带着 oo 爬山吗。</p><p><img src="https://dl4.weshineapp.com/gif/20170906/72ae6e4e617a5ea4bf6e8f301f801343.gif?f=micro_54is5bGx" alt="img"></p>]]></content>
      
      
      <categories>
          
          <category> cscore </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA_OO </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>BP</title>
      <link href="/2026/06/27/ML_%E7%90%86%E8%AE%BA_3_svmpcdpda/"/>
      <url>/2026/06/27/ML_%E7%90%86%E8%AE%BA_3_svmpcdpda/</url>
      
        <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script>]]></content>
      
      
      <categories>
          
          <category> cscore </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA_ML </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>BP</title>
      <link href="/2026/06/27/ML_%E7%90%86%E8%AE%BA_2_%E5%86%B3%E7%AD%96%E6%A0%91/"/>
      <url>/2026/06/27/ML_%E7%90%86%E8%AE%BA_2_%E5%86%B3%E7%AD%96%E6%A0%91/</url>
      
        <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script>]]></content>
      
      
      <categories>
          
          <category> cscore </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA_ML </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>BP</title>
      <link href="/2026/06/27/ML_%E7%90%86%E8%AE%BA_1_BP/"/>
      <url>/2026/06/27/ML_%E7%90%86%E8%AE%BA_1_BP/</url>
      
        <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script>]]></content>
      
      
      <categories>
          
          <category> cscore </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA_ML </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>OS_lab4</title>
      <link href="/2026/05/12/OS_Lab4/"/>
      <url>/2026/05/12/OS_Lab4/</url>
      
        <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><h2 id="font-color-ligblue-内核在哪里-font"><font color = "ligblue">内核在哪里</font></h2><p>这其实是早在上上个 Lab 就该明白的问题，但由于本人的懒惰和得过且过当时想想算了就这样吧。</p><p>内核并非只有一个（指针指着）。虽然对我来说内核很像一群不靠谱的小朋友背后的大家长，一旦小朋友（用户进程）做了什么非法操作（访问不该访问的东西）或者打起来（都想拿 CPU）就要弹出来收拾残局，然后默默撤离。但实际上，内核和用户进程并非众星拱月的关系，而是进程包含内核的关系。**内核是存在于所有进程地址空间中的一段代码，每个进程的页表里，都包含对同一份内核空间的映射。**这样相比于 “众星拱月” 的好处在于陷入内核时不必费劲切换到另一个地址空间（经历了上个 Lab 的狼狈就知道这个操作挺麻烦的）。当然虽然内核映射一直存在于进程页表里，但平时是不能访问的，要陷入内核才能访问。</p><p>当然，这只是操作系统的一种设计，并非绝对，不过现在的主流操作系统一般都是这样设计的。</p><blockquote><p>进程与内核间的关系并非对立：在内核处理进程发起的系统调用时，我们并没有切换地址空间（页目录地址），也不需要将进程上下文（Trapframe）保存到进程控制块中，只是切换到内核态下，执行了一些内核代码。可以说，处理系统调用时的内核仍然是代表当前进程的，这也是系统调用、TLB 缺失等同步异常与时钟中断等异步异常的本质区别</p></blockquote><h2 id="font-color-ligblue-系统调用（System-Call）-font"><font color = "ligblue">系统调用（System Call）</font></h2><p>内核态是被信任的，内核态允许一切指令运行，对硬件设备有完整的控制权。而用户态是不被信任的，用户态能运行的指令是内核态的一个子集。以此类推，在用户态想运行差集中的指令时，就需要陷入内核，syscall 就是这一过程的包装。将一部分用户态不允许但会频繁使用的指令包装成函数，由用户态请求，切换到内核态执行，这就是系统调用。</p><p>所以说系统调用是用户向操作系统请求的接口。但用户程序是写在 API 上而非直接写在系统调用上的，API 是更加通用的协议，比如说 C 标准库，这样设计使得程序更便于移植。</p><p>​            <img src="https://raw.githubusercontent.com/yhua10/Bed/Blog-OS/image-20260502163310519.png" width="50%"></p><h2 id="font-color-ligblue-写时复制-font"><font color = "ligblue">写时复制</font></h2><p>按道理来讲，子进程有着和父进程一摸一样的虚拟地址空间（继承来的），但这个虚拟空间会映射到不同的物理页，这意味着在 <code>fork</code> 时需要把父进程的所有物理页面复制一遍。</p><p>但事实上，子进程很可能根本不会去写这些页面中的绝大多数，既然如此，为什么不让父子进程共享一部分物理页呢？写时复制就是偷懒先不复制物理页，只是把他们标记为 <code>COW</code>（写时复制）、<code>PTE_D = 0</code>（不可写），等到父子进程中的任何一个想要写这个页面时再复制一份，把父子进程的物理页分开。</p><p>从 COW 状态到普通状态的过渡由 TLB Mod 异常处理实现（TLB Mod 异常由试图对 PTE_D = 0 的页面进行写入操作触发，现在分为普通的只读页面和 COW 页面）。</p><p><img src="https://raw.githubusercontent.com/yhua10/Bed/Blog-OS/os_01(1).png" alt=""></p><h2 id="font-color-ligblue-用户态异常处理栈-UXSTACK-font"><font color = "ligblue">用户态异常处理栈 <code>UXSTACK</code></font></h2><p>作用：<strong>让用户态也能安全处理异常，尤其是 COW 写入异常。</strong></p><p>普通用户栈 <code>USTACKTOP</code>：用于局部变量、普通函数调用</p><p>异常处理栈 <code>UXSTACKTOP</code>：专门放异常处理时的 Trapframe，专门为 cow_entry 这类异常处理函数做运行时栈</p><p>为什么普通用户栈不够用？因为普通用户栈也是页面，也可能被标记为 COW。触发 COW 写异常时，很可能普通用户栈正是触发异常的地方，这时候再让它处理该异常，就会再次触发异常，陷入无限递归。</p><p>所以专门搞一个不可能被标记为 COW 的 UXSTACKTOP 来处理这类 TLB Mod 异常，确保稳定安全。</p><h2 id="font-color-ligblue-TLB-Mod-异常-font"><font color = "ligblue">TLB Mod 异常</font></h2><p>内核允许用户进程在用户态处理 TLB Mod 异常，因此，当异常发生时，内核不直接处理 COW，而是把异常现场放在用户异常处理栈上，然后 EPC 给到用户态异常处理函数。以上操作由 <code>kerntlbex.c</code> 中的 <code>do_tlb_mod</code> 函数执行，该函数允许异常重入（在把 tf 压入 UXSTACKTOP 时会检测当前位置，如果已经在  UXSTACKTOP 里，说明正在发生异常重入，此时让 UXSTACKTOP 沿原方向继续生长即可），并根据用户是否注册了异常处理程序来决定是该跳到异常处理程序还是该 panic。</p><h2 id="font-color-ligblue-系统调用实现-font"><font color = "ligblue">系统调用实现</font></h2><p>下面三个函数都和写时映射有很强的关系。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">sys_mem_alloc 新建映射</span><br><span class="line">sys_mem_map   共享映射</span><br><span class="line">sys_mem_unmap 解除映射</span><br></pre></td></tr></table></figure><h3 id="sys-mem-alloc">sys_mem_alloc</h3><p>这个函数是用户进程申请内存的系统调用的实现。可以为自己或者自己的直接子进程申请一页内存。为什么要为子进程申请内存？很可能会在 fork 子进程刚创建还不能运行时使用。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">sys_mem_alloc</span><span class="params">(u_int envid, u_int va, u_int perm)</span> &#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">env</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Page</span> *<span class="title">pp</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (is_illegal_va(va)) &#123;</span><br><span class="line"><span class="keyword">return</span> -E_INVAL;</span><br><span class="line">&#125;</span><br><span class="line">try(envid2env(envid, &amp;env, <span class="number">1</span>));</span><br><span class="line">try(page_alloc(&amp;pp));</span><br><span class="line"><span class="keyword">return</span> page_insert(env-&gt;env_pgdir, env-&gt;env_asid, pp, va, perm);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="sys-mem-map">sys_mem_map</h3><p>是用户进程共享页面的系统调用的实现，src 进程把 srcva 映射到的物理页共享给 dst 进程的 dstva。这样一来两个进程都可以操作该物理页面，十分的危险，所以经常被用做 “写时复制”，权限设置为 COW 而非 PTE_D。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">sys_mem_map</span><span class="params">(u_int srcid, u_int srcva, u_int dstid, u_int dstva, u_int perm)</span> &#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">srcenv</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">dstenv</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Page</span> *<span class="title">pp</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (is_illegal_va(srcva) || is_illegal_va(dstva)) &#123;</span><br><span class="line"><span class="keyword">return</span> -E_INVAL;</span><br><span class="line">&#125;</span><br><span class="line">try(envid2env(srcid, &amp;srcenv, <span class="number">1</span>));</span><br><span class="line">try(envid2env(dstid, &amp;dstenv, <span class="number">1</span>));</span><br><span class="line"><span class="comment">// 找到物理页</span></span><br><span class="line">pp = page_lookup(srcenv -&gt; env_pgdir, srcva, <span class="literal">NULL</span>);</span><br><span class="line"><span class="keyword">if</span> (pp == <span class="literal">NULL</span>) &#123;</span><br><span class="line"><span class="keyword">return</span> -E_INVAL;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> page_insert(dstenv-&gt;env_pgdir, dstenv-&gt;env_asid, pp, dstva, perm);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="sys-mem-unmap">sys_mem_unmap</h3><p>为自己或者自己的子进程解除映射，很可能也是写时复制的一部分，因为 COW 异常之后 COW 的地址就没用了。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">sys_mem_unmap</span><span class="params">(u_int envid, u_int va)</span> &#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">e</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (is_illegal_va(va)) &#123;</span><br><span class="line"><span class="keyword">return</span> -E_INVAL;</span><br><span class="line">&#125;</span><br><span class="line">try(envid2env(envid, &amp;e, <span class="number">1</span>));</span><br><span class="line">page_remove(e -&gt; env_pgdir, e -&gt; env_asid, va);</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="sys-exofork">sys_exofork</h3><p>复制出一个还不能运行的半成品子进程，fork 调用的两个返回值就是在这里实现的。</p><p>这里需要注意的是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">e-&gt;env_tf = *((<span class="keyword">struct</span> Trapframe *)KSTACKTOP - <span class="number">1</span>);</span><br></pre></td></tr></table></figure><p>它的作用是把父进程（当前所在进程）陷入系统调用之前的运行现场复制给子进程。</p><p>因为栈是向低地址增长的，所以 <code>KSTACKTOP - sizeof(struct Trapframe)</code> 的位置，正好放着当前进程的 <code>Trapframe</code>。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">sys_exofork</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">e</span>;</span></span><br><span class="line">try(env_alloc(&amp;e, curenv -&gt; env_id));</span><br><span class="line">e -&gt; env_tf = *((<span class="keyword">struct</span> Trapframe *)KSTACKTOP - <span class="number">1</span>);</span><br><span class="line">e -&gt; env_tf.regs[<span class="number">2</span>] = <span class="number">0</span>;</span><br><span class="line">e -&gt; env_status = ENV_NOT_RUNNABLE;</span><br><span class="line">e -&gt; env_pri = curenv -&gt; env_pri;</span><br><span class="line"><span class="keyword">return</span> e-&gt;env_id;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后，父与子就能够分别各自从 syscall_exofork 中返回。</p><h3 id="duppage">duppage</h3><p>duppage 是 fork 里用来把父进程的一页复制给子进程的函数。</p><p>不但要给子进程设置为 COW 权限，还要覆盖自己原有的写权限。</p><h2 id="font-color-ligblue-思考题-font"><font color = "ligblue"> 思考题</font></h2><h3 id="Thinking-4-1">Thinking 4.1</h3><blockquote><p>• 内核在保存现场的时候是如何避免破坏通用寄存器的？</p></blockquote><p>通过 <code>SAVE_ALL</code> 宏把通用寄存器都收到内核栈的 TrapFrame，恢复现场的时候在一一放出来，期间内核可以自由使用寄存器而不用担心破坏用户进程。注意这里是保存到内核栈而非用户进程 PCB，<code>PCB -&gt; tf</code> 是为较长时间（等待 CPU 或阻塞）存储准备的，而系统调用通常不会切换进程，如果频繁地写入 PCB 会很麻烦，而存储到内核栈帧是最合适的。</p><blockquote><p>• 系统陷入内核调用后可以直接从当时的 <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>a</mi><mn>0</mn><mo>−</mo></mrow><annotation encoding="application/x-tex">a0-</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.72777em;vertical-align:-0.08333em;"></span><span class="mord mathnormal">a</span><span class="mord">0</span><span class="mord">−</span></span></span></span>a3 参数寄存器中得到用户调用 msyscall留下的信息吗？</p></blockquote><p>不能。如上文所述，系统调用开始时 $a0 ~ $a3 的值也会被保存到 TrapFrame 里，而 $a0 ~ $a3 本身供内核程序自由使用，可能被覆盖，因此必须从 TrapFrame 里读取用户调用 msyscall留下的信息。</p><p>准确来说：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">sysno = tf-&gt;regs[<span class="number">4</span>]; <span class="comment">// $a0</span></span><br><span class="line">arg1  = tf-&gt;regs[<span class="number">5</span>]; <span class="comment">// $a1</span></span><br><span class="line">arg2  = tf-&gt;regs[<span class="number">6</span>]; <span class="comment">// $a2</span></span><br><span class="line">arg3  = tf-&gt;regs[<span class="number">7</span>]; <span class="comment">// $a3</span></span><br></pre></td></tr></table></figure><blockquote><p>• 我们是怎么做到让 sys 开头的函数“认为”我们提供了和用户调用 msyscall 时同样的参数的？</p></blockquote><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">u_int arg4 = *(u_int *)(tf-&gt;regs[<span class="number">29</span>] + <span class="number">16</span>);</span><br><span class="line">u_int arg5 = *(u_int *)(tf-&gt;regs[<span class="number">29</span>] + <span class="number">20</span>);</span><br></pre></td></tr></table></figure><p><code>do_syscall</code> 手动模拟了一次普通的 C 函数调用</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">tf-&gt;regs[<span class="number">2</span>] = func(arg1, arg2, arg3, arg4, arg5);</span><br></pre></td></tr></table></figure><p>所以 <code>sys_*</code> 以为自己收到了和用户调用 <code>msyscall</code> 时相同的参数。</p><blockquote><p>• 内核处理系统调用的过程对 Trapframe 做了哪些更改？这种修改对应的用户态的变化是什么</p></blockquote><ul><li><p>返回到下一条指令处：</p>  <figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">tf -&gt; cp0_epc += <span class="number">4</span>;</span><br></pre></td></tr></table></figure></li><li><p>返回值：</p>  <figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">tf -&gt; regs[<span class="number">2</span>] = res;</span><br></pre></td></tr></table></figure></li></ul><h3 id="Thinking-4-2">Thinking 4.2</h3><blockquote><p>为什么 <code>envid2env</code> 中需要判断 <code>e-&gt;env_id != envid</code>？如果没有这步判断会发生什么？</p></blockquote><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">envid2env</span><span class="params">(u_int envid, <span class="keyword">struct</span> Env **penv, <span class="type">int</span> checkperm)</span> &#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">e</span>;</span></span><br><span class="line">...</span><br><span class="line">    e = &amp;envs[ENVX(envid)];</span><br><span class="line"><span class="keyword">if</span> (e-&gt;env_status == ENV_FREE || e-&gt;env_id != envid) &#123;</span><br><span class="line"><span class="keyword">return</span> -E_BAD_ENV;</span><br><span class="line">&#125;</span><br><span class="line">...</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>e-&gt;env_id != envid</code> 本质上是 <code>env_status == ENV_FREE</code> 的一种特殊情况。</p><p>因为 <code>ENVX(envid)</code> 得到的是其在 <code>envs[]</code> 中的下标，而 <code>envs[]</code> 中的槽位可以被反复使用。假设进程 A 已被销毁，A 原本在 <code>envs[]</code> 中的位置被进程 B 获取，这时候如果对 A 的 <code>env_id</code> 执行 <code>envid2env</code> 就会获得进程 B 的进程控制块，而 B 的 <code>env_status</code> 不一定是 <code>ENV_FREE</code>，如果不检查  <code>env_status == ENV_FREE</code> 就会命中错误地进程，导致严重的权限错误。</p><h3 id="Thinking-4-3">Thinking 4.3</h3><blockquote><p>请回顾 kern/env.c 文件中 mkenvid() 函数的实现，该函数不会返回 0，请结合系统调用和 IPC 部分的实现与</p><p>envid2env() 函数的行为进行解释。</p></blockquote><p>envid = 0 在 MOS 中有特殊的含义：0 表示当前进程。如果真正的进程 ID 也能为 0，那就无从区分当前进程和 ID = 0 的进程（或许也可以区分，但麻烦）。</p><h3 id="Thinking-4-4">Thinking 4.4</h3><blockquote><p>关于 fork 函数的两个返回值，下面说法正确的是：</p><p>A、fork 在父进程中被调用两次，产生两个返回值</p><p>B、fork 在两个进程中分别被调用一次，产生两个不同的返回值</p><p>C、fork 只在父进程中被调用了一次，在两个进程中各产生一个返回值</p><p>D、fork 只在子进程中被调用了一次，在两个进程中各产生一个返回值</p></blockquote><p>C。</p><p><code>fork</code> 确实只在父进程中被调用，但因为子进程会复制父进程的运行现场，使得它看起来也像是刚从 <code>fork</code> 返回，所以会有两个返回值。其实只是子进程继承了父进程的 fork 调用现场。</p><h3 id="Thinking-4-5">Thinking 4.5</h3><blockquote><p>上述 “最小版” 策略要求你在遍历时跳过 UXSTACK 与 UVPT/VPT/VPD 等特殊区域。请结合 kern/env.c 中 env_init 的映射行为、include/mmu.h 的内存布局图，解释：</p></blockquote><blockquote><p>为什么子进程的 UXSTACK 必须重新分配，而不能共享或设为 COW？</p></blockquote><p>直接共享肯定不行，因为父子进程同时发生异常的话，压进去的 TrapFrame 会互相覆盖。</p><p>设成 COW 也不行，因为 COW 本身就是依赖于异常栈来处理，如果异常栈也是 COW 的，那么处理 COW 异常时又会再次触发 COW，造成无限递归。所以 UXSTACK 必须稳定可写。</p><blockquote><p>为什么不应对 UVPT/VPT/VPD 等自映射区域调用 duppage？如果误处理，可能引发哪些不一致或错误行为？</p></blockquote><p>UVPT/VPT/VPD 是页表自映射区域，用来让用户只读查看自己的页表和页目录。</p><p><font color = "green"><strong>所以为什么呢？不知道啊</strong></font></p><h3 id="Thinking-4-6">Thinking 4.6</h3><blockquote><p>在遍历地址空间存取页表项时你需要使用到 vpd 和 vpt 这两个指针，请参考 user/include/lib.h 中的相关定义，思考并回答这几个问题：</p><p>• vpt 和 vpd 的作用是什么？怎样使用它们？</p><p>• 从实现的角度谈一下为什么进程能够通过这种方式来存取自身的页表？</p><p>• 它们是如何体现自映射设计的？</p><p>• 进程能够通过这种方式来修改自己的页表项吗？</p></blockquote><ul><li><p>vpt 指向页表窗口的起始处，用 vpt[vpn] 获取页表项。</p><p>vpd 指向页表窗口中页目录起始处，用 vpd[pdx] 获取页目录项。</p>  <figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; PDX(UXSTACKTOP); i ++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (vpd[i] &amp; PTE_V) &#123;</span><br><span class="line">        ......</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>实现了自映射。</p></li><li><blockquote><p>正常情况下，页表是内核管理的数据结构，用户程序不能直接访问。</p><p>自映射的思想是：</p><blockquote><p>把页目录本身也映射进虚拟地址空间，于是页表结构也能通过虚拟地址访问。</p></blockquote><p><code>vpd</code> 对应页目录视图，<code>vpt</code> 对应页表视图。它们本质上都是通过 UVPT 这一类特殊虚拟地址窗口访问页表结构。</p></blockquote></li><li><p>不能，一般是只读映射。</p></li></ul><h3 id="Thinking-4-7">Thinking 4.7</h3><blockquote><p>在 do_tlb_mod 函数中，你可能注意到了一个向异常处理栈复制 Trapframe 运行现场的过程，请思考并回答这几个问题：</p><p>• 这里实现了一个支持类似于“异常重入”的机制，而在什么时候会出现这种“异常重入”？</p><p>• 内核为什么需要将异常的现场 Trapframe 复制到用户空间？</p></blockquote><ul><li>用户进程正在用户异常处理程序中运行时，又一次触发写入异常</li><li>因为 COW 的具体处理在用户态完成</li></ul><h3 id="Thinking-4-8">Thinking 4.8</h3><blockquote><p>在用户态处理页写入异常，相比于在内核态处理有什么优势？</p></blockquote><p>内核只负责异常转发，不负责异常处理</p><p>这符合微内核的思想：</p><ul><li><p>（如果可以的话），便于移植</p></li><li><p>用户态出问题只影响该进程，不会破坏整个内核</p></li><li><blockquote><p>内核只提供异常分发和系统调用支持，复杂策略放在用户态实现</p></blockquote></li></ul><h3 id="Thinking-4-9">Thinking 4.9</h3><blockquote><p>为什么在设置写时复制保护机制之前，需通过 syscall_set_tlb_mod_entry 设置父进程页写入异常处理函数？若在写时复制保护机制完成之后再设置会怎样？</p></blockquote><p>因为把共享页面设置为 COW 后，父进程自己也被禁止写入（!PTE_D）。如果在写时复制保护机制完成之后再设置，则可能导致在保护机制设置过程中，父进程试图写入该页面，由于此时写时复制保护机制还未完全建立，该事件可能会触发非法写入 panic 而非 COW 异常。</p>]]></content>
      
      
      <categories>
          
          <category> cscore </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA_OS </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>OS_lab1</title>
      <link href="/2026/05/01/os_lab1/"/>
      <url>/2026/05/01/os_lab1/</url>
      
        <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><h3 id="Thinking-1-1">Thinking 1.1</h3><hr><blockquote><p>尝试分别使用实验环境中的原生 x86 工具链（gcc、ld、readelf、objdump 等）和 MIPS 交叉编译工具链（带有mips-linux-gnu- 前缀，如 mips-linux-gnu-gcc、mips-linux-gnu-ld），重复其中的编译和解析过程，观察相应的结果，并解释其中向 objdump 传入的参数的含义。</p></blockquote><p>写了一个 <code>night.c</code> 程序作为实验对象：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span> <span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">printf</span> (<span class="string">&quot;Night fall...\n&quot;</span>);</span><br><span class="line">        <span class="built_in">printf</span> (<span class="string">&quot;Sleep well, wake well.&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">gcc -E night.c</span><br></pre></td></tr></table></figure><p>前面补上一大堆 <code>typedef</code> 、<code>struct</code> 和 <code>extern</code></p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">gcc -c night.c</span><br></pre></td></tr></table></figure><p><code>night.o</code> ：</p><p><img src="https://github.com/yhua10/Bed/blob/main/image-20260325000727645.png?raw=true" alt="image-20260325000727645"></p><p>反汇编：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">objdump -DS night.o &gt; night.s</span><br></pre></td></tr></table></figure><p><img src="https://github.com/yhua10/Bed/blob/main/image-20260325001139378.png?raw=true" alt="image-20260325001139378"></p><p>文件格式：elf64-x86-64</p><p>用交叉编译工具链 gcc + 反汇编出的是  elf32-tradbigmips，看起非常亲切（终于有个能看懂的了）</p><p><img src="https://github.com/yhua10/Bed/blob/main/image-20260325112659865.png?raw=true" alt="image-20260325112659865"></p><p><code>readelf</code></p><p><img src="https://github.com/yhua10/Bed/blob/main/image-20260325113044240.png?raw=true" alt="image-20260325113044240"><img src="https://github.com/yhua10/Bed/blob/main/image-20260325113101285.png?raw=true" alt="image-20260325113101285"></p><p><img src="https://github.com/yhua10/Bed/blob/main/image-20260325113159790.png?raw=true" alt="image-20260325113159790"></p><p>发现 Entry Point 不同，正常，ABI 不同，start_ 也不同；</p><p>发现连 段头/节头 的数目都不同，MIPS 少一些。</p><p><code>objdump</code> 中的参数：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-S# 尽量 “源代码 + 汇编混合显示”，但我没加 -g 调试模式，所以没起效</span><br><span class="line">-D# 所有 section，不止 .text</span><br></pre></td></tr></table></figure><h3 id="Thinking-1-2">Thinking 1.2</h3><hr><blockquote><p>• 尝试使用我们编写的 readelf 程序，解析之前在 target 目录下生成的内核 ELF 文件。</p><p>• 也许你会发现我们编写的 readelf 程序是不能解析 readelf 文件本身的，而我们刚才介绍的系统工具 readelf 则可以解析，这是为什么呢？（提示：尝试使用 readelf-h，并阅读 tools/readelf 目录下的 Makefile，观察 readelf 与 hello 的不同）</p></blockquote><p><strong>实验：</strong></p><p>用我们编写的 <code>readelf</code> 文件解析 <code>target/mos</code> 的结果是：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">0:0x0</span><br><span class="line">1:0x80020000</span><br><span class="line">2:0x800220b0</span><br><span class="line">3:0x800220c8</span><br><span class="line">4:0x800220e0</span><br><span class="line">5:0x0</span><br><span class="line">6:0x0</span><br><span class="line">7:0x0</span><br><span class="line">8:0x0</span><br><span class="line">9:0x0</span><br><span class="line">10:0x0</span><br><span class="line">11:0x0</span><br><span class="line">12:0x0</span><br><span class="line">13:0x0</span><br><span class="line">14:0x0</span><br><span class="line">15:0x0</span><br><span class="line">16:0x0</span><br><span class="line">17:0x0</span><br><span class="line">18:0x0</span><br></pre></td></tr></table></figure><p>而用系统工具 <code>readelf</code> 解析结果是（我用的是 <code>-a</code> ，太长了，省略部分）：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line">ELF 头：</span><br><span class="line">  Magic：   7f 45 4c 46 01 01 01 00 00 00 00 00 00 00 00 00 </span><br><span class="line">  类型:                              EXEC (可执行文件)</span><br><span class="line">  系统架构:                          MIPS R3000</span><br><span class="line">  入口点地址：               0x80021c60</span><br><span class="line">  程序头起点：          52 (bytes into file)</span><br><span class="line">  Start of section headers:          23976 (bytes into file)</span><br><span class="line">  Size of this header:               52 (bytes)</span><br><span class="line">  Size of program headers:           32 (bytes)</span><br><span class="line">  Number of program headers:         4</span><br><span class="line">  Size of section headers:           40 (bytes)</span><br><span class="line">  Number of section headers:         19</span><br><span class="line">节头：</span><br><span class="line">  [Nr] Name              Type            Addr     Off    Size   ES Flg Lk Inf Al</span><br><span class="line">  [ 0]                   NULL            00000000 000000 000000 00      0   0  0</span><br><span class="line">  [ 1] .text             PROGBITS        80020000 0000c0 0020b0 00 WAX  0   0 16</span><br><span class="line">.......</span><br><span class="line">程序头：</span><br><span class="line">  Type           Offset   VirtAddr   PhysAddr   FileSiz MemSiz  Flg Align</span><br><span class="line">  ABIFLAGS       0x002188 0x800220c8 0x800220c8 0x00018 0x00018 R   0x8</span><br><span class="line">  REGINFO        0x002170 0x800220b0 0x800220b0 0x00018 0x00018 R   0x4</span><br><span class="line">......</span><br><span class="line">There is no dynamic section in this file.</span><br><span class="line">该文件中没有重定向信息</span><br><span class="line">......</span><br><span class="line">ISA: MIPS32</span><br><span class="line">GPR size: 32</span><br><span class="line">CPR1 size: 32</span><br><span class="line">CPR2 size: 0</span><br><span class="line">FP ABI: Hard float (32-bit CPU, Any FPU)</span><br><span class="line">.......</span><br></pre></td></tr></table></figure><p>系统 <code>readelf</code> 解析我的 <code>readelf</code>：</p><p><img src="https://github.com/yhua10/Bed/blob/main/image-20260325135528080.png?raw=true" alt="image-20260325135528080"></p><p>我发现我的 <code>readelf</code> 有重定位节。</p><p><img src="https://github.com/yhua10/Bed/blob/main/image-20260325140345360.png?raw=true" alt="image-20260325140345360"></p><p>我的 <code>readelf</code> 自我解析 &amp;&amp; 我的 <code>readelf</code> 解析系统 <code>readelf</code>：</p><p><img src="https://github.com/yhua10/Bed/blob/main/image-20260325142737403.png?raw=true" alt="image-20260325142737403"></p><p>解析出来俩空文件。</p><p>系统的 <code>readelf</code> 自我解析：</p><p><img src="https://github.com/yhua10/Bed/blob/main/image-20260325142542674.png?raw=true" alt="image-20260325142542674"></p><p><strong>思考：</strong></p><ul><li></li><li>对哦，我们的 <code>readelf</code> 全是针对 32 位的，但它自己能在宿主机运行，那肯定是 64 位的，所以解析不了。</li><li>系统的 “通用 readelf“ 自然是通用的。</li></ul><h3 id="Thinking-1-3">Thinking 1.3</h3><hr><blockquote><p>在理论课上我们了解到，MIPS 体系结构上电时，启动入口地址为 0xBFC00000（其实启动入口地址是根据具体型号而定的，由硬件逻辑确定，也有可能不是这个地址，但一定是一个确定的地址），但实验操作系统的内核入口并没有放在上电启动地址，而是按照内存布局图放置。思考为什么这样放置内核还能保证内核入口被正确跳转到？</p><p>（提示：思考实验中启动过程的两阶段分别由谁执行。）</p></blockquote><ul><li>pre：两阶段由谁执行：<ul><li>CPU 固定复位到 BootLoader，这个由硬件执行。</li><li>BootLoader 引导 CPU 跳到内核，这个由 BootLoader 执行。</li></ul></li><li>所以为什么：<ul><li>CPU 本身不认识内核入口，但 BootLoader 认识，BootLoader 会通过 ELF 头找到和内核的契约，然后引导 CPU 跳过去。</li><li>欸？所以 BootLoader 也是一种 <code>readelf</code> ？还是说它只是接受 <code>readelf</code> 解析出的结果？🤔<ul><li>关键 BootLoader 起作用的时候 <code>readelf</code> 还不在运行链路里啊。<ul><li>GPT 说 BootLoader 包含一小段和 <code>readelf</code> 相似的逻辑，来解析 ELF 头。</li></ul></li></ul></li></ul></li></ul><h3 id="交叉编译：">交叉编译：</h3><p>编译器生成的 <strong>目标代码</strong> 与 <strong>编译器本身</strong> 在不同的平台上运行。为什么要交叉编译？因为 QEMU 模拟的是 MIPS 平台环境，我们的 MOS 是在 MIPS 上跑的；而我们的跳板机显然是在 X86 上跑的。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">gcc</span><br><span class="line">mips-linux-gnu-gcc # 用一个运行在 x86 上的程序，生成给 MIPS 用的程序</span><br></pre></td></tr></table></figure><h3 id="QEMU-的简化版启动：">QEMU 的简化版启动：</h3><blockquote><p>QEMU 模拟器支持直接加载 ELF 格式的内核，也就是说，QEMU 已经提供了 bootloader 的引导（启动）功能。MOS 操作系统不需要再实现 bootloader的功能。</p></blockquote><p>我们在理论课上学习了相当复杂的启动流程，主要包括 ① BootLoader 引导 ② 硬件环境逐步复苏 ③ 将内核加载到内存中的指定位置 ④ CPU 跳转到内核入口执行。而 QEMU 模拟器帮我们实现了大部分，启动流程被简化为 ③④。</p><h3 id="启动">启动</h3><p><code>init/start.S</code> 中的 <code>_start</code> 函数是 CPU 控制权被转交给内核后执行的第一个函数，主要工作是初始化 CPU 和栈指针，为之后的内核初始化做准备。最后跳到 <code>init.c</code> 中的 <code>mips_init</code>。观察 <code>mips_init</code> 发现它只是一个占位的东西，还没有实现任何内核相关内容。</p><p>所以其实我们 lab1 干的事儿跟内核啥关系没有。</p><h3 id="Makefile：">Makefile：</h3><p>上来就是一句：<code>.ONESHELL:</code> 意味着我们可以不去注意 Makefile 中 “换行即换 shell“ 这件事。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">Lab1</span><br><span class="line">  |- Makefile</span><br><span class="line">  |- lib</span><br><span class="line">    |- Makefile</span><br><span class="line">  |- init</span><br><span class="line">    |- Makefile</span><br><span class="line">  |- kernel</span><br><span class="line">    |- Makefile</span><br></pre></td></tr></table></figure><p>Makefile 里的东西很多很杂，而且不乏涉及到一些与实验无关内容，我没有试图完全理解，下面挑重点描述：</p><figure class="highlight makefile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">target_dir              := target<span class="comment"># 最终的 MOS 构建目标所在目录</span></span><br><span class="line">mos_elf                 := <span class="variable">$(target_dir)</span>/mos<span class="comment"># 我们要完成的操作系统其实就是个 ELF 文件</span></span><br><span class="line">link_script             := kernel.lds</span><br><span class="line"><span class="comment"># Linker Script 是用来指导链接器将多个 .o 文件链接成目标可执行文件的脚本</span></span><br><span class="line">modules                 := lib init kern<span class="comment"># 子模块</span></span><br><span class="line">targets                 := <span class="variable">$(mos_elf)</span></span><br></pre></td></tr></table></figure><p>简要阅读 Makefile 即可发现，其中存在大量 “指来指去” 的内容，化简完如下：</p><figure class="highlight makefile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">all: <span class="variable">$(mos_elf)</span></span></span><br><span class="line"><span class="variable">$(mos_elf)</span>: <span class="variable">$(modules)</span> <span class="variable">$(target_dir)</span><span class="comment"># $(LD) 链接所有目标文件</span></span><br><span class="line"><span class="variable">$(LD)</span> <span class="variable">$(LDFLAGS)</span> -o <span class="variable">$(mos_elf)</span> -N -T <span class="variable">$(link_script)</span> <span class="variable">$(objects)</span></span><br><span class="line"><span class="variable">$(target_dir)</span>:</span><br><span class="line">mkdir -p <span class="variable">$@</span></span><br><span class="line"><span class="variable">$(modules)</span>: <span class="comment"># 进入各个子目录（子模块）进行 Make</span></span><br><span class="line"><span class="variable">$(MAKE)</span> --directory=<span class="variable">$@</span></span><br><span class="line"><span class="section">clean:</span></span><br><span class="line">for d in * tools/readelf user/* tests/*; do <span class="comment"># 遍历所有目录，寻找其内部 Makefile 并执行 clean</span></span><br><span class="line">if [ -f $$d/Makefile ]; then</span><br><span class="line"><span class="variable">$(MAKE)</span> --directory=$$d clean</span><br><span class="line">fi</span><br><span class="line">done</span><br><span class="line">rm -rf *.o *~ <span class="variable">$(target_dir)</span> <span class="keyword">include</span>/generated</span><br><span class="line">find . -name &#x27;*.objdump&#x27; -exec rm &#123;&#125; &#x27;;&#x27;</span><br><span class="line"><span class="section">run:</span></span><br><span class="line"><span class="variable">$(QEMU)</span> <span class="variable">$(QEMU_FLAGS)</span> -kernel <span class="variable">$(mos_elf)</span></span><br></pre></td></tr></table></figure><p>注意这里没有像通常那样把 <code>all</code> 作为第一个（默认）规则，而是 <code>clean-and-all</code>，可能是出于方便考虑，我们无需手动清理之前生成的副产物，直接 <code>make</code> 就行。注意评测命令是 <code>make &amp;&amp; make run</code>。</p><img src="https://github.com/yhua10/Bed/blob/main/image-20260324231150102.png?raw=true"  /><h3 id="变长参数列表">变长参数列表</h3><p>比如这段代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">printk</span><span class="params">(Const chsr *fmt, ...)</span> &#123;</span><br><span class="line">    va_list ap;</span><br><span class="line">    va_start(ap, fmt);</span><br><span class="line">    vprintfmt(outputk, <span class="literal">NULL</span>, fmt, ap);</span><br><span class="line">    va_end(ap);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">va_list ap; <span class="comment">// 变长参数</span></span><br></pre></td></tr></table></figure><p>初始化变长参数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">va_start(ap, fmt <span class="comment">/* 指向 ... 前的最后一个参数 */</span>); </span><br></pre></td></tr></table></figure><p>（连续多次地）使用 <code>va_arg</code> 来接受 … 里的参数，比如 <code>va_arg(ap, int);</code> 就会读到一个 <code>int</code> 类型的值。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> a = va_arg(ap <span class="comment">/*...*/</span>, <span class="type">int</span> <span class="comment">/* 指定接受参数的类型 */</span>);</span><br><span class="line"><span class="type">int</span> b = va_arg(ap, <span class="type">long</span>);</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>注意这里最少接受 4 个字节的参数，<code>char</code> 不允许。</p><p>最后：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">va_end(ap);</span><br></pre></td></tr></table></figure><blockquote><p>有关为什么 <code>printk</code> 为什么要使用回调函数，因为输出函数不只有 <code>printk</code>，（还有 <code>print</code> ,<code>printf</code>, <code>sprintf</code>……），都使用 <code>vprintfmt</code> 接口，通过指定（设计）回调函数来指定（设计）输出方式。</p></blockquote><h3 id="ELF">ELF:</h3><blockquote><p>先看 <code>elf.h</code>。</p></blockquote><img src="https://upload.wikimedia.org/wikipedia/commons/7/77/Elf-layout--en.svg" alt="Executable and Linkable Format - Wikipedia" style="zoom:30%;" /><blockquote><p>P.S. 这张图我忘了是哪儿来的了，可能是从哪位学长或者学姐的博客里拿来的……反正不是我自己画的</p></blockquote><h3 id="C-语言相关">C 语言相关</h3><p>C 语言环境分为 <code>Freestanding</code> 和 <code>Hosted</code> 两种，前者有操作系统支持，后者没有。显然没有操作系统支持的 <code>Freestanding</code> 不可能支持 <code>I/O</code> 操作，所以 我们使用的 <code>Freestanding</code> C 语言环境 + 我们实现的 <code>I/O</code> 操作 ➡️ 能进行 I/O 操作的 C 语言环境。</p><p>独立环境下能使用的头文件：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">&lt;<span class="type">float</span>.h&gt;  </span><br><span class="line">&lt;iso646.h&gt;</span><br><span class="line">&lt;limits.h&gt;</span><br><span class="line">&lt;stdarg.h&gt;</span><br><span class="line">&lt;stdbool.h&gt;</span><br><span class="line">&lt;stddef.h&gt;</span><br><span class="line">&lt;stdint.h&gt;</span><br></pre></td></tr></table></figure><p>然后我们下发的代码又给我们补了一些，比如说 <code>string.h</code>，里面的也是我们能直接用的。</p><h3 id="回调函数-printk">回调函数 &amp;&amp; printk</h3><p>这四个代码一眼看上去有点乱。<code>printk.c</code> 和 <code>print.c</code> 如胶似漆（各种相互调用），<code>print.h</code> 和 <code>printk.h</code> 存在感模糊。</p><p><code>printk.c</code> 的 <code>outputk</code> 到 <code>print.c</code> 里还改了个名字叫做 <code>out</code>，我一开始在根目录下 <code>grep -r &quot;out&quot;</code> 啥也没找着。</p><blockquote><p>我后来明白为什么要改名字了。<code>outputk</code> 只是 <code>printk</code> 对应的输出方式（输出到 stdout），如果，我要加一个 <code>sprinfmt</code>，我给它写一个，比方说，<code>soutput</code>，那 <code>soutput</code> 也要走 <code>out</code> 接口，如果还管他叫 <code>outputk</code> 就不合适了。</p></blockquote><p>其实就是这样：⬇️</p><img src="https://github.com/yhua10/Bed/blob/main/image-20260326002037397.png?raw=true" /><p>回调函数首先是在 <code>print.h</code> 中出现的：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="title function_">void</span> <span class="params">(*<span class="type">fmt_callback_t</span>)</span><span class="params">(<span class="type">void</span> *data, <span class="type">const</span> <span class="type">char</span> *buf, <span class="type">size_t</span> len)</span>;</span><br></pre></td></tr></table></figure><p>这里命名了一个函数指针类型 <code>fmt_callback_t</code>，其指向的函数格式为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">somedef</span><span class="params">(<span class="type">void</span> *data, <span class="type">const</span> <span class="type">char</span> *buf, <span class="type">size_t</span> len)</span></span><br></pre></td></tr></table></figure><p>它被用在 <code>vprintfmt</code> 里：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">vprintfmt</span><span class="params">(<span class="type">fmt_callback_t</span> out, <span class="type">void</span> *data, <span class="type">const</span> <span class="type">char</span> *fmt, va_list ap)</span>;</span><br></pre></td></tr></table></figure><p>这里的注释挺详细的，奈何我不懂英文。</p><p>注释：</p><blockquote><p><code>vprintfmt</code> 是一个格式化函数，它允许使用不同的后端（即输出目标）来完成打印。它接收四个参数：</p><ul><li><p><code>fmt_callback_t out</code>：一个指向输出目标的函数指针，这个输出目标函数负责接收 <code>vprintfmt</code> 生成的格式化输出。</p></li><li><p><code>void *data</code>：一个上下文指针，会被传递给 <code>out</code> 回调函数。它可以用来保存与具体输出目标相关的额外数据。</p></li><li><p><code>const char *fmt</code>：格式字符串，和 <code>printf</code> 的格式字符串类似。</p></li><li><p><code>va_list ap</code>：可变参数列表，提供要被格式化的那些参数。</p></li></ul><p>格式化回调函数 <code>out</code> 接收下面这些参数：</p><ul><li><code>void *data</code><ul><li>就是传给 <code>vprintfmt</code> 的那个同一个 <code>data</code> 指针。</li></ul></li><li><code>const char *buf</code><ul><li>指向一个缓冲区，这个缓冲区里存放的是 <code>vprintfmt</code> 生成好的格式化输出。</li></ul></li><li><code>size_t len</code><ul><li>这个缓冲区中有多少字节有效。</li></ul></li></ul><p>注意，这个缓冲区不一定以 <code>\0</code> 结尾，而且里面还可能包含嵌入的 <code>\0</code> 字节，因此输出目标在处理时，应该把 <code>len</code> 当作真正需要输出的长度。</p><blockquote><p>也就是说，这里的字符串输出并非像我们平时用的读到 ‘\0’ 自动结束，而是从 buf[0] 到 buf[len-1]，输完为止。</p></blockquote></blockquote><p>具体表现为 <code>printk.c</code> 中的：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* 正 */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">outputk</span><span class="params">(<span class="type">void</span> *data, <span class="type">const</span> <span class="type">char</span> *buf, <span class="type">size_t</span> len)</span> &#123;</span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; len; i++) &#123;</span><br><span class="line">printcharc(buf[i]);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 误 */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">outputk</span><span class="params">(<span class="type">void</span> *data, <span class="type">const</span> <span class="type">char</span> *buf, <span class="type">size_t</span> len)</span> &#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>, buf);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>本质上就是 <code>print.c</code> 和 <code>printk.c</code> 里函数的相互调用。</p><p>这里的 <code>*fmt</code> 都是用户程序提供的格式化字符串，比如说 <code>&quot;%s, %-4d&quot;</code>，<code>*buf</code> 都是整理好准备输出的字符串，比如说 <code>&quot;hello, 3   &quot;</code>。</p><p>这里还有个很有意思的事儿，先把负数取绝对值再连同负号输出，这遇到 <code>LONG_MIN</code> 不就炸了吗。</p><h3 id="scancharc"><code>scancharc</code></h3><p>lab1 没有任何涉及到输入的上层实现，但我发现 <code>machine.c</code> 里有一个孤零零的 <code>scancharc</code>，它和 <code>printcharc</code> 差不多，都是最底层的实现。</p><p>我仿照 <code>outputk</code> 写了一个 <code>inputk</code>：</p><p><img src="https://github.com/yhua10/Bed/blob/main/inputk.png?raw=true" alt="image-20260329143343268"></p>]]></content>
      
      
      <categories>
          
          <category> cscore </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA_OS </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>神秘 bug</title>
      <link href="/2026/03/29/%E7%A5%9E%E7%A7%98bug/"/>
      <url>/2026/03/29/%E7%A5%9E%E7%A7%98bug/</url>
      
        <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><hr><p>在注入里修改了夜间模式下的头像，后又突发奇想在本土 iml 下启用了头像旋转效果。然后我得到了……一轮旋转的满月。</p><p>特此纪念。</p><center><video src="/img/moon.mp4" controls width="500"></video></center><center><video src="/img/or.mp4" controls width="500"></video></center>]]></content>
      
      
      <categories>
          
          <category> else </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 一些有趣的事情 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hello World</title>
      <link href="/2026/01/23/hello-world/"/>
      <url>/2026/01/23/hello-world/</url>
      
        <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start">Quick Start</h2><h3 id="Create-a-new-post">Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server">Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files">Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites">Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p><div class="note default flat"><p>默认提示块</p></div><div class="note blue icon-padding flat"><i class="note-icon fas fa-info-circle"></i><p>蓝色提示块</p></div><div class="note green flat"><p>绿色提示块</p></div><div class="note red flat"><p>红色警告块</p></div><div class="note orange flat"><p>橙色提示块</p></div><div class="note purple flat"><p>紫色提示块</p></div>]]></content>
      
      
      
    </entry>
    
    
  
  
</search>
